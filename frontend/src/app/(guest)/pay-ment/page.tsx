import React from 'react'
import Payment from './Payment'

export const metadata = {
    title: "Payment | SUIT SHOP",
    description: "Secure and fast payment processing.",
};

const page = () => {
    return (
        <div>
            <Payment />
        </div>
    )
}

export default page